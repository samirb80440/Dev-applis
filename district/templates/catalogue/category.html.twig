{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}

<div class="container-fluid p-0">
     <div id="parent">
         <div id="banniere" class="row g-0">
             <img class="imagebanniere img-fluid" src="{{ asset('images_the_district/fosyyyy.jpg') }}" alt="banniere">
         </div>
     </div>
 </div>
<div class="container g-0">
    
    <div class="fs-1  ms-md-5 red">Nos Catégories :</div>

        <div class="g-0 p-0 row justify-content-center">


      {% for categorie in categories %}
        <div class="carde col-12 col-sm-6 col-md-4 mt-3">

      <div class="carde col-12 col-sm-6 col-md-4 mt-3 ">
                     <a  href="{{ path ('app_platcat' , {categorie_id: categorie.id} ) }}">
                            <img src="{{ asset ('images_the_district/category/' ~ categorie.image) }}" class=" imagecat" alt="{{ categorie.libelle }}">
                            <div class="card-body">
                            </div>
                        </a>
     </div>
                    </div>

                    {% endfor %}

                </div>
                </div>
                </div>


{% endblock %}